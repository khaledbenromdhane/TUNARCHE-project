{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <h2>Détail Quiz</h2>

    <p><strong>Titre :</strong> {{ quiz.title }}</p>
    <p><strong>Formation :</strong> {{ quiz.formation.title }}</p>

    <a href="{{ path('app_admin_quiz_edit', {'id': quiz.id}) }}" class="btn btn-warning">Modifier</a>
    <a href="{{ path('app_admin_quiz_index') }}" class="btn btn-secondary">Retour</a>
</div>
{% endblock %}