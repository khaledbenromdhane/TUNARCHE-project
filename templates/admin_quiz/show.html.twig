{% extends 'back/base_admin.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ quiz.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_quiz_index') }}">back to list</a>

    <a href="{{ path('app_admin_quiz_edit', {'id': quiz.id}) }}">edit</a>

    {{ include('admin_quiz/_delete_form.html.twig') }}
{% endblock %}
